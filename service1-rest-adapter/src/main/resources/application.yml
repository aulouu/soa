server:
  port: ${SERVER_PORT:9090}

spring:
  application:
    name: service1-rest-adapter

# SOAP Service URL
soap:
  service:
    url: http://localhost:8082/service1-soap/HumanBeingService

  connection:
    timeout: 10000

  request:
    timeout: 10000

logging:
  level:
    itmo.soa.adapter.client.SoapClientService: INFO
    org.apache.cxf: WARN
