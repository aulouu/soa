# Публичный доступ к SOA Lab 3

## Быстрый старт (одна команда!)

```bash
./start-public.sh
```

Этот скрипт:
1. ✅ Запускает все backend сервисы
2. ✅ Создаёт Cloudflare Tunnel для API Gateway
3. ✅ Создаёт Cloudflare Tunnel для Frontend  
4. ✅ Настраивает Frontend на использование публичного API URL
5. ✅ Запускает Frontend

---

## Что вы получите

После запуска вы увидите:

```
🌐 ПУБЛИЧНЫЕ URL (доступны из интернета):
  Frontend:      https://xxxxx.trycloudflare.com
  API Gateway:   https://yyyyy.trycloudflare.com
```

### Публичный Frontend
- Открывается в браузере из любой точки мира
- Автоматически подключён к публичному API Gateway
- Работает через HTTPS

### Публичный API Gateway  
- Доступен для curl/Postman из интернета
- Все микросервисы доступны через Gateway
- CORS настроен для работы с Frontend

---

## Примеры использования

### Через Frontend
Просто откройте публичный URL Frontend в браузере:
```
https://xxxxx.trycloudflare.com
```

### Через API напрямую
```bash
# Human Beings
curl https://yyyyy.trycloudflare.com/api/human-beings

# Heroes
curl https://yyyyy.trycloudflare.com/api/heroes

# Health check
curl https://yyyyy.trycloudflare.com/actuator/health
```

---

## Остановка

```bash
./start.sh --stop
```

---

## Как это работает

```
Интернет → Cloudflare Tunnel (Frontend) → localhost:3000 → React App
                                             ↓
                                  REACT_APP_API_URL
                                             ↓
Интернет → Cloudflare Tunnel (Gateway) → localhost:8080 (Zuul) → Сервисы
```

### Cloudflare Tunnel
- Создаёт безопасный туннель от Cloudflare до вашего localhost
- Не требует открытия портов в firewall
- Автоматически получает HTTPS сертификат
- Бесплатный временный URL (quick tunnel)

### CORS
- Настроен в Zuul Gateway для приёма запросов с любых origin
- Обрабатывает OPTIONS preflight запросы
- Добавляет все необходимые заголовки Access-Control-*

---

## Troubleshooting

### Frontend не видит данные
**Проблема:** CORS ошибки в консоли браузера

**Решение:** Перезапустите Frontend после запуска туннелей:
```bash
# Остановить frontend
kill $(cat logs/frontend.pid)

# Запустить заново
cd frontend && npm start > ../logs/frontend.log 2>&1 & echo $! > ../logs/frontend.pid
```

### Туннель не создаётся
**Проблема:** cloudflared не запускается

**Решение:** Проверьте, что cloudflared установлен:
```bash
cloudflared --version
```

Если нет, запустите:
```bash
./setup.sh
```

### 530 ошибка от Cloudflare
**Проблема:** Cloudflare не может подключиться к backend

**Решение:** 
1. Проверьте, что Zuul Gateway запущен: `curl -k https://localhost:8080/actuator/health`
2. Проверьте логи: `tail logs/cloudflared-gateway.log`
3. Перезапустите туннель: `./start.sh --stop && ./start-public.sh`

---

## Локальная разработка

Если вам не нужен публичный доступ:

```bash
# Только backend
./start.sh --backend

# Backend + Frontend (localhost)
./start.sh --dev
```

---

## Конфигурация

Включить/выключить Cloudflare в `config.env`:
```bash
CLOUDFLARED_ENABLED=true  # или false
```

Количество инстансов сервисов:
```bash
SERVICE1_INSTANCES=2  # 1 или 2
SERVICE2_INSTANCES=2  # 1 или 2
```
